# Sentinel environment variables

Copy this file to `.env.local` for local Next.js development.
Never commit real secrets.

## Application

# local | staging | production
SENTINEL_ENV=local
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_VERSION=0.1.0

## Database (Cloudflare D1 — configure when Worker/D1 is wired)

# Local D1 binding is configured in worker/wrangler.toml
# For remote/staging, set account/database IDs via Wrangler secrets — not here.
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_D1_DATABASE_ID=
# CLOUDFLARE_API_TOKEN=

## Auth (self-hosted; values generated locally — do not reuse in production)

# AUTH_SECRET=generate-a-long-random-string
# AUTH_URL=http://localhost:3000

## Optional BYOK AI detectors (disabled by default — leave unset)

# SENTINEL_AI_DETECTOR_ENABLED=false
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

## Cost protection / kill switches

# SENTINEL_GLOBAL_KILL_SWITCH=false
# SENTINEL_MAX_TOOL_CALLS_PER_ORG_MONTH=10000

## Demo / attack simulator

# SENTINEL_DEMO_ENABLED=true
# SENTINEL_ATTACK_SIMULATOR_ENABLED=true
