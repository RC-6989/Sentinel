{
  "name": "@sentinel/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker API gateway for Sentinel",
  "scripts": {
    "dev": "echo \"Wrangler not configured yet — Phase 4. Use apps/web for local API routes.\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.8.3"
  }
}
