# sentinel-sdk (Python)

Placeholder package. Full client lands in Phase 15 after the gateway exists.

```python
from sentinel_sdk import Sentinel

sentinel = Sentinel(api_key="sn_live_...")
# sentinel.execute("issue_refund", {"amount": 129})  # NotImplementedError until Phase 15
```
